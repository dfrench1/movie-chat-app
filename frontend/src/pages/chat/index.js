import React from 'react';
import ChatRoom from '../../components/chat/index'

const Chat = props => {
    return (
        <ChatRoom {...props} />
    );
};

Chat.propTypes = {
    
};

export default Chat
